# 帮助文档输出格式
```bash
[binaryName] version [version]
Usage:   
    -command     [params]

Commands and Options:
    第一项参数为默认参数。
    -open   使用参数中的选项打开对应的浏览器，可以为数组
        chrome
        firefox
    -config 指定配置文件
        filePath
```